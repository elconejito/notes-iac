# === Terraform ===
*.tfstate
*.tfstate.*
crash.log
.terraform/
# We REMOVED .terraform.lock.hcl from here so it CAN be committed
terraform.tfvars
*.tfvars

# === Ansible ===
*.retry
*.log
ansible/retry/
# Only ignore inventory if it's auto-generated/sensitive
# inventory.ini 

# === Environment & Secrets ===
.env
.env.*
!.env.example

# === System & IDE ===
.DS_Store
.idea/
.vscode/
.cursor/
*.bak
*.swp

# === Docker/Local Data ===
# We ignore the local data folder so your 1GB of notes 
# doesn't get pushed to your GitHub repository!
ansible/data/
/opt/notes-stack/data/
